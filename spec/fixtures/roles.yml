# == Schema Information
#
# Table name: roles
#
#  id          :bigint(8)        not null, primary key
#  description :string
#  name        :string           not null
#

one:
  name: admin
  description: admin role

two:
  name: my_contacts
  description: my_contacts role
